<?php
// Подключается в начале каждой защищённой страницы админки.
// Отдельного входа для панели больше нет (см. admin-x7k9m2/login.php) —
// доступ даёт обычный вход на сайте (site_users) с флагом "администратор"
// (Настройки → «Администраторы»), поэтому и отправляем сюда же. next=
// — чтобы после входа человек попал не на главную сайта, а сразу туда,
// куда и шёл (см. обработку next в /login.php).
$__requestedPage = $_SERVER['REQUEST_URI'] ?? '/admin-x7k9m2/dashboard.php';
requireAdmin('../login.php?next=' . urlencode($__requestedPage));
